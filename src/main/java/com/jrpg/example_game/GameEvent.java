package com.jrpg.example_game;

//probably make the eventName into an enum

//(comment hours after previous comment)
//definitely make this into an enum in the actual implementation
public interface GameEvent {
    String getEventName();
}
